/*const fs=require('fs');
fs.writeFile(`${__dirname}/pet.txt`,'Boxer',(err) => {
    if(err){
        console.log("Error Happened")
        return;
    }
    console.log("Saved data successfully");
})
*/

const fs=require('fs');
fs.writeFile(`${__dirname}/pet1.txt`,'Boxer',(err) => {
    if(err){
        console.log("Error Happened")
        return;
    }
    console.log("Saved data successfully");
})